# Runtime files — generated during play, not part of the repo
display/app.log
display/app.pid
display/stats.json
display/text_log.json
display/.token
display/tracker.json

# SRD data — downloaded at runtime via scripts/data_pull.py, not committed
data/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/

# macOS
.DS_Store
